# Portable training data

The generated `bitext-v1/` directory contains MLX-LM-compatible `train.jsonl`,
`valid.jsonl`, and `test.jsonl`. The same logical chat records can be consumed
by a future TRL/PEFT implementation.
