{{/* Conditional content. skip globs match POST-strip output names and are
relative to this file's directory (template/ root).

The Databricks Apps deployment path requires a Databricks model provider
because the externally onboarded App identity must call a Databricks serving
endpoint. Route Foundry through a Databricks external-model endpoint first,
then select provider databricks. Foundry projects therefore omit the deployment
path regardless of include_serving. */}}
{{- if or (eq .include_serving "no") (eq .model_provider "foundry") -}}
{{ skip "notebooks/02_enable_monitoring.py" }}
{{ skip "resources/agent_app.yml" }}
{{ skip "app.yaml" }}
{{ skip "requirements.txt" }}
{{ skip "start_server.py" }}
{{ skip "src/app/endpoint.py" }}
{{ skip "tests/test_app_endpoint.py" }}
{{- end -}}
{{ skip "__preamble" }}
