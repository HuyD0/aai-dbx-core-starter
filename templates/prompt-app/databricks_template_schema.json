{
  "welcome_message": "Create an AAI prompt application\n\nGoverned prompt lifecycle: register versions, evaluate pinned versions with LLM judges, promote aliases only after the gate passes, and deploy a traced assistant.",
  "min_databricks_cli_version": "v1.6.0",
  "properties": {
    "project_name": {
      "type": "string",
      "default": "aai-prompt-app",
      "description": "Project and bundle name. NOTE: apply template updates deliberately \u2014 generated projects record their template version in .aai-template.json",
      "order": 1,
      "pattern": "^[a-z][a-z0-9-]+$",
      "pattern_match_failure_message": "Use lowercase letters, numbers, and hyphens."
    },
    "application_name": {
      "type": "string",
      "default": "prompt-assistant",
      "description": "Application identifier used in tags and traces",
      "order": 2
    },
    "repository_url": {
      "type": "string",
      "description": "Required absolute HTTPS URL of this project's real Git repository; Hub registration rejects example and placeholder URLs",
      "order": 22,
      "pattern": "^https://[^\\s@/?#]+(?:/[^\\s?#]*)?$",
      "pattern_match_failure_message": "Use an absolute HTTPS repository URL without credentials, spaces, a query string, or a fragment."
    },
    "team": {
      "type": "string",
      "default": "data-platform",
      "description": "Owning team",
      "order": 3
    },
    "owner_group": {
      "type": "string",
      "default": "group:data-platform-owners",
      "description": "Non-personal owning group",
      "order": 4
    },
    "cost_center": {
      "type": "string",
      "default": "CC-1234",
      "description": "Non-secret finance cost center",
      "order": 5
    },
    "catalog": {
      "type": "string",
      "default": "main",
      "description": "Unity Catalog catalog (also hosts the prompt registry)",
      "order": 6
    },
    "schema": {
      "type": "string",
      "default": "prompt_apps",
      "description": "Unity Catalog schema for the prompt registry",
      "order": 7
    },
    "model_provider": {
      "type": "string",
      "default": "databricks",
      "description": "Model provider. Choose `databricks` (recommended default: gateway-ready serving endpoint) unless your team already operates its own Foundry resource. Application code only uses the logical name",
      "enum": [
        "databricks",
        "foundry"
      ],
      "order": 8
    },
    "model_deployment": {
      "type": "string",
      "default": "replace-with-model-endpoint",
      "description": "Databricks serving endpoint name or Foundry deployment name",
      "order": 9
    },
    "foundry_endpoint": {
      "type": "string",
      "default": "https://replace-with-foundry-account.services.ai.azure.com/api/projects/replace-with-project",
      "description": "Foundry project endpoint ending in /api/projects/<project> (only asked for Foundry models)",
      "order": 10,
      "skip_prompt_if": {
        "properties": {
          "model_provider": {
            "const": "databricks"
          }
        }
      }
    },
    "judge_deployment": {
      "type": "string",
      "default": "replace-with-judge-endpoint",
      "description": "Approved gateway-fronted Databricks serving endpoint used by LLM judges",
      "order": 11
    },
    "prompt_name": {
      "type": "string",
      "default": "assistant_system",
      "description": "Registered prompt name in the Unity Catalog prompt registry",
      "order": 12,
      "pattern": "^[a-z][a-z0-9_]+$",
      "pattern_match_failure_message": "Use lowercase letters, numbers, and underscores."
    },
    "aai_core_version": {
      "type": "string",
      "default": "0.4.0",
      "description": "Pinned aai-core SDK version",
      "order": 16
    },
    "aai_core_volume": {
      "type": "string",
      "default": "/Volumes/dbx_dev/dbx_platform/python_packages",
      "description": "Unity Catalog volume holding aai-core wheels",
      "order": 17
    },
    "workspace_host": {
      "type": "string",
      "default": "https://adb-7405609799238491.11.azuredatabricks.net",
      "description": "Development Azure Databricks workspace",
      "order": 18
    },
    "compute_policy_id": {
      "type": "string",
      "default": "0005F2031B6D2319",
      "description": "Constrained Job Compute policy",
      "order": 19
    },
    "aai_core_pip_source": {
      "type": "string",
      "default": "git+https://github.com/HuyD0/aai-dbx-core-starter@v{{.aai_core_version}}",
      "description": "pip-installable aai-core source used only by credential-free CI; MUST resolve to exactly aai_core_version (default: the release git tag; for an internal index use 'aai-core=={{.aai_core_version}}'). Runtime jobs always install the checksum-pinned volume wheel",
      "order": 20
    }
  },
  "success_message": "\n{{.project_name}} created.\n\nNext steps:\n  1. cd {{.project_name}}\n  2. python3.12 scripts/setup_dev.py              # preflight + isolated setup\n  3. make check                                   # tests + deterministic gate\n  4. python scripts/register_prompt.py            # registers v1, sets development alias\n  5. make evaluate                                # LLM-judge gate on the pinned version\n  6. python scripts/promote_prompt.py --version 1 --to validation\n\nRun `make help` for every available target."
}
