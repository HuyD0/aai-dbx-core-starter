{
  "$comment": "Versioned prompt source. Edit here, register with scripts/register_prompt.py (a new immutable version), evaluate the pinned version, then promote. The {{question}} placeholder is an MLflow prompt variable, filled at format() time.",
  "commit_message": "Assistant system prompt",
  "messages": [
    {
      "role": "system",
      "content": "You are a helpful, truthful assistant. Answer concisely from what you know; when you are not sure, say so explicitly instead of guessing. Refuse requests for unsafe or private information."
    },
    {
      "role": "user",
      "content": "{{question}}"
    }
  ]
}
