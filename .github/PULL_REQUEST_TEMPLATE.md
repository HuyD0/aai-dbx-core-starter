## Outcome

Describe the user-visible result and the SDK/template/example area affected.

## Compatibility and security

- [ ] Public API, schema, dependency, and migration impacts are documented.
- [ ] Pull-request checks remain credential-free and no sensitive data was added.
- [ ] Native provider access remains available; any new abstraction has at least two stable consumers.

## Verification

List the exact checks run, including relevant render variants and cancellation,
failure, or rollback cases. Note any credentialed validation that must run only
after merge to protected `main`.
